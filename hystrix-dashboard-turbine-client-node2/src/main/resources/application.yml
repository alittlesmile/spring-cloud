server:
  port: 9003
spring:
  application:
    name: hystrix-dashboard-client-node2

eureka:
  client:
    service-url:
      defaulZone: http://localhost:8761/eureka/
      
management:
  endpoints:
    web:
      exposure:
        include: '*'

feign:
  hystrix:
    enabled: true