server:
  port: 8000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://localhost:3380/root/smile.git
          search-paths: config-repo
          username: root
          password: root123456
          
