server:
  port: 8007
spring:
  application:
    name: consumer-feign-hystrix
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
      
feign:
  hystrix:
    enabled: true